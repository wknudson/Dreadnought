{
  "name": "dreadnought",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "license": "AGPL-3.0-only",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types --test tests/*.test.ts",
    "data": "node --experimental-strip-types tools/convert-tanks.ts",
    "check": "npm run data && npm test && npm run build",
    "balance": "node --experimental-strip-types tools/balance.ts",
    "finale": "node --experimental-strip-types tools/finale.ts"
  },
  "devDependencies": {
    "@types/node": "^22.20.3",
    "typescript": "^7.0.2",
    "vite": "^8.3.0"
  }
}
